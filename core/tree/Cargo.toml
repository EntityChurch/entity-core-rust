[package]
name = "entity-tree"
version = "0.10.0"
edition.workspace = true
description = "system/tree handler: get, put, listing"

[dependencies]
entity-ecf.workspace = true
entity-hash.workspace = true
entity-entity.workspace = true
entity-handler.workspace = true
entity-store.workspace = true
entity-types.workspace = true
entity-capability.workspace = true
# §5.4 byte fidelity: `encode_envelope` / `decode_envelope` / `cbor_map_field_raw`
# are the only codec in this tree that carries an entity's `data` as RAW bytes.
# `extract` and `merge` both inline entities, so both must go through them.
entity-wire.workspace = true
async-trait.workspace = true
thiserror.workspace = true
ciborium.workspace = true
sha2.workspace = true
tracing.workspace = true

[dev-dependencies]
entity-crypto.workspace = true
tokio.workspace = true
