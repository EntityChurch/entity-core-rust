[package]
name = "entity-protocol"
version = "0.9.0"
edition.workspace = true
description = "EXECUTE dispatch, connection handshake, authentication"

[dependencies]
entity-ecf.workspace = true
entity-hash.workspace = true
entity-entity.workspace = true
entity-crypto.workspace = true
entity-types.workspace = true
entity-capability.workspace = true
entity-wire.workspace = true
entity-handler.workspace = true
ciborium.workspace = true
rand.workspace = true
thiserror.workspace = true
tracing.workspace = true
# §5.2 capability expiry needs a wall clock, and `std::time::SystemTime::now()`
# PANICS on wasm32-unknown-unknown ("time not implemented on this platform").
# `web_time` re-exports the std types natively, so this changes nothing off-wasm.
web-time.workspace = true
